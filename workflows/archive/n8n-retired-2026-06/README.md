# Retired n8n Workflow Archive

This folder contains historical n8n exports and sync metadata from the earlier QBR workflow implementation.

n8n is no longer part of the active local Advertiser QBR runtime. The current flow is:

```text
Chrome extension / TD app -> advertiser-qbr-local-runner -> qbr-pptx-service
```

Keep these files only for reference, comparison, or recovery work. Do not use them as the source of truth for current setup or runtime behavior unless the user explicitly asks to restore the n8n path.
